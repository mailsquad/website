box: centos:7

build:
  steps:
    - script:
       name: Install docker
       code: |
         sudo yum -y install docker

    - script:
       name: Build docker image
       code: |
         sudo service docker start
         sudo docker build -t mailsquad/website .